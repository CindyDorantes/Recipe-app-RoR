<section class="container text-center">
  <h2 class="mb-5">
    <%= @recipe.name %>
  </h2>

  <div class="container text-start">
    <p>Preparation time: <%= @recipe.preparation_time %></p>
    <p>Cooking time: <%= @recipe.cooking_time %></p>
    <p>Steps go here...</p>
  </div>

  <div class="container text-start m-5">
    <% if @recipe.user_id == current_user.id %>
      <%# <%= link_to shopping_list_path do %> %>
        <%# <button class="btn btn-primary mb-3">Generate shopping list</button> %>
      <%# <% end %> %>
      <%= link_to new_recipe_recipe_food_path(@recipe.id) do %>
        <button class="btn btn-primary mb-3">Add ingredient</button>
      <% end %>
    <% end %>
  </div>

  <table class="table table-striped table-hover">
    <tr>
      <th scope="col">Food</th>
      <th scope="col">Quantity</th>
      <th scope="col">Value</th>
      <th scope="col">Actions</th>
    </tr>
    <% @recipe_foods.each do |recipe_food| %>
      <%= render partial: "shared/recipe_ingredients", locals: { recipe_food: recipe_food } %>
    <% end %>
  </table>

</section>